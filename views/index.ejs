<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900">
        <div class="form">
            <form action="/create" class="flex flex-col items-center " method="post">
                <input class="mt-5 w-1/2 outline-none px-2 py-2" type="text" name="title" placeholder="Title...">
                <textarea class="mt-2 outline-none w-1/2 px-2 py-2 resize-none" name="details" id="" placeholder="Enter Text"></textarea>
                <button type="submit" class="mt-2 bg-blue-500 px-3 py-2 rounded-sm text-white font-bold">Create Task</button>
            </form>
        </div>
        <div class="mt-10 flex flex-wrap gap-3 justify-center" >
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="w-72 rounded-sm bg-zinc-700 text-white px-2">
                        <h1 class="text-xl "><%= val %></h1>
                        <a href="/files/<%= val %>" target="_blank"  class="text-blue-500 inline-block my-2">Read More</a>
                    </div>
                <% }) %>
            <% }else{ %>
                <h4 class="text-zinc-500 text-xl">No task yet</h4>
            <% } %>
            
        </div>
        
    </div>
</body>
</html>